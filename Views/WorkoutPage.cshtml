@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage

@{
    Layout = "mainLayout.cshtml";
    
    // Load CMS-created workout templates from Umbraco
}

<section class="">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">

        <div class="mt-8">
            @* @await Html.PartialAsync("_FilterBar") *@

            @* Placeholder for CMS workout templates *@
            <div class="text-center py-12">
                <div class="max-w-md mx-auto">
                    <i class="fas fa-dumbbell text-6xl text-gray-300 mb-4"></i>
                    <h3 class="text-xl font-semibold text-gray-700 mb-2">Workout Templates Coming Soon!</h3>
                    <p class="text-sm text-gray-500 mt-4">
                        For now, try our <a href="/workouts/generator" class="text-primary hover:underline">AI Workout Generator</a> 
                        to create custom workouts!
                    </p>
                </div>
            </div>
            

            @await Html.PartialAsync("_LoadingState")
            @await Html.PartialAsync("_EmptyState")
        </div>
    </div>
</section>