@inherits UmbracoViewPageBase<IPublishedContent>

@{
    var searchPage = Model.Root().DescendantsOfType("searchPage").FirstOrDefault();

    var loginLink = GetPageUrl("loginPage", "/login");
    var signUpLink = GetPageUrl("signUpPage", "/signup");

    var header = searchPage?.Value("exersiceModalHeader") ?? "Login Required";
    var text = searchPage?.Value("exersiceModalText") ?? "You need to log in or sign up to save items.";
    var loginText = searchPage?.Value("exersiceModalLogin") ?? "Login";
    var signupText = searchPage?.Value("exersiceModalSignUp") ?? "Sign up";
    var closeText = searchPage?.Value("exersiceModalClose") ?? "Close";

}

<div id="loginModal" class="fixed inset-0 bg-black/50 flex items-center justify-center hidden z-50">
    <div class="bg-white rounded-lg shadow-lg p-8 max-w-sm w-full text-center">
        <div class="relative mb-4">
            <h2 class="text-xl font-semibold text-gray-800 text-center">
                @header
            </h2>
            <button id="closeLoginModal" class="absolute right-0 top-0 text-red-600 hover:text-red-800 font-bold">
                <i class="fa-solid fa-xmark fa-lg"></i>
            </button>
        </div>

        <p class="text-gray-600 mb-6">@text</p>
        <div class="flex justify-center gap-4 text-white">
            <a href="@loginLink" class="bg-primary px-4 py-2 rounded-md hover:bg-primaryHover transition">@loginText</a>
            <a href="@signUpLink" class="bg-primary px-4 py-2 rounded-md hover:bg-primaryHover transition">@signupText</a>
        </div>
    </div>
</div>
